enum Segment
{
    LCL,
    ARG,
    THIS,
    THAT,
    STATIC,
    CONSTANT,
    TEMP,
};

enum ArithmeticOperator {
    ADD,
    SUB,
    NEG
};

enum ComparisonOperator {
    EQ,
    GT,
    LT,
};

enum LogicalOperator {
    AND,
    OR,
    NOT
};